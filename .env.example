# Copiar como .env para desarrollo local con backend PostgreSQL.
# Sin esto, DB_PASSWORD queda vacío y la conexión a Postgres falla
# (el ejecutable empaquetado no lo necesita: usa SQLite local).

DB_NAME=app_presupuestos
DB_USER=postgres
DB_PASSWORD=
DB_HOST=localhost
DB_PORT=5432
